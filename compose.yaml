services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: portfolio:latest
    container_name: portfolio
    restart: unless-stopped
    # Host port is overridable so a reverse proxy on the server can pick it:
    #   PORT=3000 docker compose up -d
    ports:
      - "${PORT:-8080}:80"
